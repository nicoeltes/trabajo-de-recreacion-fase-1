html, body {
    margin: 0;
    padding: 0;
    height: 100%;
    background-color: #299000;
}

.body1 {
    display: flex;
    justify-content: flex-start; /* Centra el título horizontalmente */
    align-items: flex-start;
   /* margin-top: 50px;*/       /* Espacio superior para despegarlo del borde */
}

/* Capa 2: Iniciar sesión */
.body2 {
    display: flex;
    justify-content: center; /* Centra el contenido horizontalmente */
    align-items: center;
    margin-top: 90px;        /* Distancia razonable respecto al título */
}

/* Capa 3: Crear cuenta */
.body3 {
    display: flex;
    justify-content: center; /* Centra el contenido horizontalmente */
    align-items: center;
    margin-top: 20px;        /* Distancia razonable respecto al botón superior */
}

h1 {
    width: 200px;
    height: 100px;
    display: flex;
    justify-content: center;
    align-items: center;
    text-align: center;
    font-size: 30px;
    font-weight: 200;
    background-color: #FCEDFF;
    color: #1d1f1d;
    border-radius: 30px;
    margin-top: -21px;
    margin-left: -16px;
    /* Transición para el título */
    transition: transform 0.3s ease, background-color 0.3s ease;
}

/* Hover para el título */
h1:hover {
    background-color: #ffffff;
    transform: scale(1.03); /* Crece ligeramente al pasar el mouse */
}

.iniciarsesion {
    width: 280px;
    height: 130px;
    font-size: 32px;
    background-color: #014A00;
    color: white;
    border-color:white ;
    border-radius: 16px;
    cursor: pointer;
    /* Transiciones suaves para color, elevación y sombra */
    transition: background-color 0.3s ease, transform 0.2s ease, box-shadow 0.3s ease;

}

/* Hover para Iniciar Sesión */
.iniciarsesion:hover {
    background-color: #027400; /* Verde más claro */
    transform: translateY(-4px); /* Eleva el botón */
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.25); /* Agrega sombra */
}

.crearcuenta {
    width: 280px;
    height: 130px;
    font-size: 32px;
    background-color: #014A00;
    color: white;
    border-color: white;
    border-radius: 16px;
    cursor: pointer;
    /* Transiciones suaves para color, elevación y sombra */
    transition: background-color 0.3s ease, transform 0.2s ease, box-shadow 0.3s ease;
}

/* Hover para Crear Cuenta */
.crearcuenta:hover {
    background-color: #027400; /* Verde más claro */
    transform: translateY(-4px); /* Eleva el botón */
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.25); /* Agrega sombra */
}